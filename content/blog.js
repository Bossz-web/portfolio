/**
 * Latest blog posts shown in the Blog section grid.
 *
 * The Blog section auto-hides while this array is empty (see
 * components/sections/blog.jsx). When your blog is live, add entries here —
 * each with { title, excerpt, date, readingTime, url, tag } — and re-add the
 * { label: "Blog", href: "#blog" } item to navItems in content/personal.js.
 *
 * @type {import('@/types').BlogPost[]}
 */
export const blogPosts = [];
